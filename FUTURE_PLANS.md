# 今後の拡張と課題 (Future Plans & Known Issues)

Altair Web Controller の将来的な拡張アイデアおよび現状確認されている技術的な課題についてのまとめです。

## 1. 既存の技術的課題 (Known Issues)

現状の仕様上、以下の点についてパフォーマンス面での懸念があります（将来的な最適化の余地があります）。

- **シリアル通信のトラフィック負荷**
  - `canSerial.js` において、値の変更の有無に関わらず、10ms（MDD/Servo）や100ms（Solenoid）の間隔で定期的に送信処理が走っています。
  - 送信モジュールが増えた場合、USBシリアルの帯域やバッファを圧迫し、通信遅延やパケットロス（バッファ詰まり）の原因になる可能性があります。
  - **改善案**: 目標値や状態が変更された時のみ送信する（オンデマンド送信）、または定期送信のインターバルを動的に調整する仕組みへの移行。

- **自動化エンジン (Blockly) のCPU負荷**
  - オートトリガーエンジンON時、CANフレームを受信（`handleIncomingCanFrame`）するたびに、Blocklyから生成されたJavaScriptコードを `eval()` で実行してトリガーを評価しています。
  - CANのステータス受信頻度が高い場合、頻繁な `eval()` 実行によるCPU負荷の増大や、ガベージコレクションの頻発によるUIのフリーズが懸念されます。
  - **改善案**: `eval()` に頼らず、事前にパース・コンパイルされたAST（構文木）ベースのインタプリタで条件を評価するか、Web Workerへ処理をオフロードする仕組みの導入。

## 2. 今後の拡張アイデア (Future Extensions)

より便利で強力なシステムにするための拡張アイデアです。

### 2.1. 設定機能の強化
- **設定データのエクスポート / インポート機能**
  - 現在はブラウザのLocalStorageにのみ保存されていますが、設定データ（モジュール構成、Blocklyマクロ、Gamepadマッピング）をJSONファイルとしてローカルに保存（ダウンロード）し、他の端末やブラウザへインポートできる機能。
  - ロボットやプロジェクトごとに設定をファイル管理できるようになります。

### 2.2. UI / UXの向上
- **レスポンシブデザインの最適化**
  - 現状はPCブラウザでの利用を前提としていますが、画面サイズに応じてレイアウトを調整するレスポンシブ対応を行うことで、小型のノートPC等でも快適に操作できるようにする。
- **ダーク/ライトテーマの切り替え**
  - ユーザーの好みに応じて、現在のダークテーマとライトテーマを切り替えられる機能。

### 2.3. システム連携・遠隔操作
- **WebSocketを用いたリモートコントロール機能**
  - Web Serial APIが使えないスマートフォンやタブレットからの操作を可能にするため、Node.jsやPythonで作られた中継サーバーを立ち上げ、ブラウザからWebSocket経由で遠隔操作するモードの追加。

### 2.4. デバイスサポートの拡張
- **新しいモジュールタイプへの対応**
  - センサーモジュール（IMU、距離センサーなど）の表示・ロギング対応。
  - センサー値をBlocklyのトリガー条件として活用できるようにする拡張。
- **ゲームパッド機能の拡張**
  - 複数台のゲームパッドの同時接続・個別マッピング対応。
  - アナログスティックのデッドゾーン（遊び）をUIから詳細に設定できる機能。
